region                       = "us-east-1"
environment                  = "dev"
terraform_operator_role_name = "REPLACE_WITH_OPERATOR_ROLE_NAME"

# The base backend and this role policy deliberately share this fixed key.
state_key = "carrier-risk/base/terraform.tfstate"
